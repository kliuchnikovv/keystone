/**
 * React-обёртки над `<ks-*>`.
 *
 * Отдельный вход, потому что ядро системы фреймворк-независимо: плагин на
 * голом JS не должен тянуть React ради `<ks-button>`. React и react-dom
 * объявлены опциональными peer-зависимостями по той же причине.
 *
 * Обёртки нужны, пока apps/web на React 18: он передаёт непримитивные пропы в
 * custom element как строковые атрибуты и не умеет декларативно слушать
 * кастомные события.
 */
export {};
