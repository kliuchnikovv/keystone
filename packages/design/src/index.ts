/**
 * Публичный вход дизайн-системы.
 *
 * Импорт этого модуля регистрирует все `<ks-*>` элементы — регистрация custom
 * element'ов и есть сайд-эффект, ради которого package.json перечисляет этот
 * файл в `sideEffects`. Пометить пакет side-effect-free нельзя: бандлер вытрясет
 * вызовы customElements.define и страница отрисуется пустой.
 *
 * Компоненты появляются здесь по мере готовности; сборка держится с первого дня,
 * чтобы формат артефакта проверялся не в тот момент, когда его впервые понадобится
 * отдать плагинам.
 */
export {};
