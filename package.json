{
  "name": "keystone-monorepo",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "build": "yarn workspace @keystone/design build && yarn workspace @keystone/web build",
    "typecheck": "yarn workspaces run typecheck",
    "test": "yarn workspaces run test"
  },
  "resolutions": {
    "lit": "^3.2.0",
    "lit-html": "^3.2.0",
    "@lit/reactive-element": "^2.0.0"
  }
}
